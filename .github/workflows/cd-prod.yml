name: CD Prod

on:
  push:
    tags: ["v*"]

env:
  REGISTRY: ghcr.io
  API_IMAGE: ghcr.io/dreyssechris/choam-api
  WEB_IMAGE: ghcr.io/dreyssechris/choam-web

jobs:
  build-and-push:
    name: Build & Push Production Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME}" >> $GITHUB_OUTPUT

      - name: Build & push API image
        uses: docker/build-push-action@v6
        with:
          context: src/
          file: src/Choam.Presentation/Dockerfile
          target: prod
          platforms: linux/arm64
          push: true
          tags: |
            ${{ env.API_IMAGE }}:${{ steps.version.outputs.VERSION }}
            ${{ env.API_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push Web image
        uses: docker/build-push-action@v6
        with:
          context: src/choam-web
          file: src/choam-web/Dockerfile
          target: prod
          platforms: linux/arm64
          push: true
          tags: |
            ${{ env.WEB_IMAGE }}:${{ steps.version.outputs.VERSION }}
            ${{ env.WEB_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Checkout homelab repo
        uses: actions/checkout@v4
        with:
          repository: dreyssechris/homelab
          token: ${{ secrets.HOMELAB_PAT }}
          path: homelab

      - name: Update prod image tags in homelab
        run: |
          sed -i "s|newTag: .*|newTag: ${{ steps.version.outputs.VERSION }}|g" \
            homelab/deploy/k8s/apps/choam/overlays/prod/kustomization.yaml

      - name: Commit and push to homelab
        run: |
          cd homelab
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add deploy/k8s/apps/choam/overlays/prod/kustomization.yaml
          git diff --staged --quiet || git commit -m "chore(deploy): update choam prod to ${{ steps.version.outputs.VERSION }}"
          git push
